# ============================================
# SMSOK Clone — PRODUCTION Environment
# Server: 185.241.210.52
# Port: 3000
# ============================================

# --- Database (REQUIRED) ---
DATABASE_URL="postgresql://smsok:CHANGE_ME@localhost:5432/smsok?schema=public&connection_limit=10&pool_timeout=20"

# --- Redis (REQUIRED) ---
REDIS_URL="redis://localhost:6379"
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=CHANGE_ME  # uncomment if Redis has auth

# --- Auth (REQUIRED) ---
JWT_SECRET=CHANGE_ME_openssl_rand_hex_32
ADMIN_JWT_SECRET=CHANGE_ME_openssl_rand_hex_32_DIFFERENT_FROM_JWT_SECRET
OTP_HASH_SECRET=CHANGE_ME_openssl_rand_hex_32
# OTP_BYPASS_CODE=  # LEAVE EMPTY in production!

# --- App (REQUIRED) ---
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_APP_URL="https://YOUR_DOMAIN.com"
BACKOFFICE_APP_URL="https://admin.YOUR_DOMAIN.com"
# COMMIT_SHA=  # set by deploy script
# LOG_LEVEL=info

# --- EasyThunder SMS Gateway (REQUIRED) ---
SMS_API_URL=https://sms-api.cl1.easythunder.com
SMS_API_USERNAME=CHANGE_ME
SMS_API_PASSWORD=CHANGE_ME

# --- EasySlip Payment (REQUIRED for topup) ---
EASYSLIP_API_KEY=CHANGE_ME
EASYSLIP_API_URL=https://document.easyslip.com/documents/verify/bank/image

# --- SlipOK Payment (REQUIRED for orders) ---
SLIPOK_BRANCH_ID=CHANGE_ME
SLIPOK_API_KEY=CHANGE_ME

# --- Resend Email (REQUIRED for notifications) ---
RESEND_API_KEY=CHANGE_ME
RESEND_FROM="SMSOK <noreply@YOUR_DOMAIN.com>"

# --- 2FA Encryption (REQUIRED) ---
TWO_FA_ENCRYPTION_KEY=CHANGE_ME_openssl_rand_hex_32

# --- Cron Jobs (REQUIRED) ---
CRON_SECRET=CHANGE_ME_openssl_rand_hex_24

# --- Cloudflare R2 (REQUIRED for file storage) ---
R2_ACCOUNT_ID=CHANGE_ME
R2_ACCESS_KEY_ID=CHANGE_ME
R2_SECRET_ACCESS_KEY=CHANGE_ME
R2_ENDPOINT=https://ACCOUNT_ID.r2.cloudflarestorage.com
R2_BUCKET_SLIPS=smsok-slips
R2_BUCKET_DOCS=smsok-docs
# R2_PUBLIC_URL=https://cdn.YOUR_DOMAIN.com

# --- Sentry (recommended) ---
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/xxx
# SENTRY_ORG=your-org
# SENTRY_PROJECT=smsok-clone
# SENTRY_AUTH_TOKEN=CHANGE_ME

# --- Debug (DO NOT enable in production) ---
# DEBUG_API_ERRORS=0
# DEV_SECRET=  # LEAVE EMPTY!
